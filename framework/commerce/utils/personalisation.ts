export type PersonalisationOptions = {
  imageUrl: string;
  text: string;
  textColor: string;
  textPosition: string;
  textCoordinates: { x: number, y: number };
  fontFamily: string;
  fontSize: number;
}
